GCC				= g++
CFLAGS		= -Wall -Wextra -pedantic -std=c++0x -Weffc++ -O3 -march=native -pipe

ifdef TESTS
	CFLAGS += -DMAKE_TESTS
endif

ifdef DEBUG
	CFLAGS += -DHAVE_DEBUG
endif

%.o: %.cpp
	$(GCC) $(CFLAGS) -c $<

